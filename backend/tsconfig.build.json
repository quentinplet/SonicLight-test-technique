// Build-only config: same options, sources only — tests, seed and configs are typechecked, not built.
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "src"
  },
  "include": ["src"]
}
