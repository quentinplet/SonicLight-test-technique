# Copy to .env — .env is never committed.
# Absolute API URL, called cross-origin in development exactly as in production. No trailing slash.
# Inlined at build time: changing it on a deployed front requires a rebuild.
VITE_API_URL="http://localhost:3000"
